import math
import os
import random
import re
import sys



if __name__ == '__main__':
    n = int(input().strip())
    a_list = []

    for i in range(1, n):
        if n % i == 0:
            a_list.append(i)

    maximum = max(a_list)
    print(maximum)
